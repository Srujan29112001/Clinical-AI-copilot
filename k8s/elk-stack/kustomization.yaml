apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clinical-ai

resources:
  - elasticsearch.yaml
  - logstash.yaml
  - kibana.yaml

commonLabels:
  stack: elk
  project: clinical-ai-copilot

commonAnnotations:
  version: "8.10.0"
  compliance: "hipaa"
