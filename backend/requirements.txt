# ── Clinical AI Copilot — Backend (FastAPI multi-agent service) ──
# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# HTTP client for LLM providers (local + cloud)
httpx==0.28.1

# Numerical / signal processing (dependency-light EEG pipeline)
numpy==2.2.1
scipy==1.15.0

# Optional heavy extras (uncomment to enable full EDF parsing / advanced features)
# mne==1.9.0
# pyedflib==0.1.38
# pandas==2.2.3
