# Point the UI at your FastAPI backend to use the REAL multi-agent pipeline.
# Leave it blank/unset to run the fully-functional OFFLINE DEMO (no backend needed).
#
# Local development:
# NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Production (e.g. backend on Render/Fly/Railway):
# NEXT_PUBLIC_API_URL=https://your-backend.example.com
NEXT_PUBLIC_API_URL=
